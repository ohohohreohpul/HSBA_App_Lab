# OPTIONAL. The Admin gate is a showcase, not security — the code defaults to
# "0000" and is visible in the source. Copy this file to `secrets.toml` (same
# folder) only if you want to override that default code. It changes nothing
# about security either way.

[admin]
code = "0000"
